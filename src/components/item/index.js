import React from 'react';
import {style} from './index.scss';

const Item = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Item.propTypes = {

};

export default Item;
